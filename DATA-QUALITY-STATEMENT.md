# DATA-QUALITY-STATEMENT · 数据质量声明（二次发布承诺书）

> 核验日期：2026-09-21 · 数据集 midicn-lib（现行）· **133,667** 首 / **21** 源 · 三档：main 79,216 / piano-special 34,869 / study 19,582
>
> 本数据集为**二次整理发布**。我们承诺：以下「已验证」章节的每一项都经过程序化核验并有审计报告支撑；
> 同时诚实地列出「信任边界」——即我们**无法承诺**的部分。使用本数据集前请阅读本声明。
> 历史版本口径（v1.0 · 94,740 首 / 17 源）的原始声明见本文件末尾「版本沿革」。

## 一、已验证项（程序化核验 · 可承诺）

| # | 验证项 | 方法 | 结果（现行 133,667 首） |
|---|---|---|---|
| 1 | **文件完整性** | 发布树与 catalog 逐一比对（2026-09-21 复核：main 79,216 + piano-special 34,869 + study 19,582） | **133,667 个 MIDI 全部在位，缺失 0** |
| 2 | 同源路径唯一性 | 每源 midi.file 全量比对 | 0 冲突（哈希命名保证） |
| 3 | **MIDI 可解析性** | 各批次入库时**逐文件**以 mido 重算时长与音符数 | `du` / `nn` 覆盖 **100%**（即 133,667 个文件全部解析成功） |
| 4 | MIDI 规范性 | 文件头 + 音高范围 0–127（v1.0 期 500 首抽样） | 全部合规 |
| 5 | 记录结构 | 20 字段（19 必需 + `duplicate_of` 可选）逐一检查 | 100% 合规 |
| 6 | 重复控制 | 两阶段检测（MD5 + 音高指纹），指纹经人工抽样验证 | `duplicate_of` 标记发布时过滤；v1.5 期 giantmidi 再剔除 742 个同内容转录 |
| 7 | **许可分区** | 21 源逐源审计（`docs/LICENSE-AUDIT.md`） | main 79,216 / piano-special 34,869 / study 19,582 |
| 8 | 雷区排除 | 受版权保护的流行/游戏转录 | 0 收录（Wikifonia 5,988 首版权内容被过滤；Lakh 剔除含版权声明与流行/影视路径曲目） |
| 9 | 作曲家字段 | 归并映射经人工逐条审查（含误伤修正 + 传统曲集归位） | slug/name **100%** 覆盖 |
| 10 | 时期字段 | 音乐学共识锚点作曲家强制覆盖 + 生卒年推断 | 88.1% 覆盖（catalog 2026-09-21 统计）· 锚点错位 0 |
| 11 | **音乐内容统计级验证** | mido 解析 + 五维算法：调性相关（K-S）、动机重复率、密度、音域、时长 | **基线：v1.0 期全量 94,740 首**（正常 97.6% · broken 排除 · suspect 标记）；其后新增批次逐文件解析校验（第 3 项） |

**音乐内容验证的量化证据**（v1.0 期全量基线，94,740 首）：

| 指标 | P1 | 中位 | P99 | 含义 |
|---|---:|---:|---:|---|
| 调性相关系数 | 0.0* | **0.826** | 0.972 | *P1=0 来自 139 条 suspect（已标记），其余 98%+ 在 0.45+ |
| 动机重复率 | 0.0* | **0.485** | 1.0 | 真实音乐的乐句重复特征 |
| 音符密度/秒 | 1.2 | 4.1 | 17.6 | 合理演奏速度 |

> 报告：`docs/music-verify-report.md`（由 `tools/music_verify.py` 生成，可复跑）。

## 二、信任边界（我们无法逐一承诺的部分 · 诚实披露）

### 2.1 与原乐谱的音符级比对（唯一保留的未验证项）
- 本库的 MIDI 来自公开数据集的既有转录。我们完成了**统计级验证**（上表第 11 项：调性/动机/密度等音乐学特征检测，能发现乱码、损坏、转录断裂类问题）与**逐文件解析校验**（第 3 项）。
- 但**逐音符与原乐谱比对**（验证每个音高/时值的转录误差）需音乐学人工核听，仍未执行。
- **含义**：数据不存在「损坏/乱码/空文件」级问题（已程序化排除）；个别曲目可能存在转录误差（错音、省略反复），这继承自原始数据集。

### 2.2 元数据继承
- `title / opus / form / key` 等元数据**信任原始数据集**，并经多源富化（IMSLP 作品目录、Essen 地理字段、官方作曲家表等，累计修正数万处）。现行标题覆盖 **90.0%**；无标题曲目由「作曲家 + 编号」合成显示名，保证可分辨 100%。
- 作曲家署名信任源数据集。**已知的系统性问题已修正**（表演者误标为作曲家、归并误伤等），但长尾「1–2 首」署名未逐一人工考证。

### 2.3 链式许可
- 我们核实了**各数据集自身的许可声明**（CC BY / CC BY-SA / PD / 开放声明等）。
- 但对**上游转录者是否确实有权按该许可发布**（如 aria 的转录来源录音版权状态、Lakh 内单曲的原始权利），我们**无法穿透验证**。已按最保守策略分区（存疑内容全部排除或隔离）。

### 2.4 指纹去重的残余风险
- 「仅同作曲家」的指纹相似对**未全部处理**（可能为同曲不同版本，也可能为不同曲子）——按「宁保留不过删」原则保留，并输出待人工复核清单。

## 三、使用建议

1. **`main`（C1，79,216 首）**可放心商用，须按各来源要求署名。
2. **`piano-special`（C2，34,869 首）**为 CC BY-NC-SA 4.0：仅限非商业用途，商用需另行授权。
3. **`study`（C3，19,582 首）**仅限学习与研究：不得再分发、不得商用。
   - `chinafolk`（中国民歌 10,473 首）：旋律为传统音乐，但转录底本（《中国民间歌曲集成》记谱稿）的整理与汇编受版权保护 → 已按 C3 发布，使用须显著标注数据集与底本
   - `lakh`（9,630 首）：数据集 CC BY 4.0，但内容层经版权过滤后**保守归 C3**
4. ⚠️ **`thesession`（23,250 首）**：在 CC BY-SA 4.0 之外**附加「禁止用于大语言模型」条款**（详见 `LICENSE.md` §2.2）。
5. 对音乐学准确性有严格要求的场景（如学术研究），建议对所用曲目做人工核听。
6. 发现数据问题时请反馈至项目仓库，我们按「审计 → 修复 → 重审计」流程处理。

## 四、审计工具链（可复现）

| 工具 | 作用 |
|---|---|
| `tools/audit.py` / `audit2.py` / `audit_v3.py` | 结构层与深度层终审（v1–v3） |
| `tools/quality_pipeline.py` | 幂等质量管线（ingest 后必须重跑） |
| `tools/dedup.py` / `dedup_apply.py` | 两阶段去重检测与保守标记 |
| `tools/clean_v1.py` / `infer_period.py` | 作曲家归并 + 时期推断 |
| `tools/music_verify.py` | 音乐内容统计级验证（生成 `midi_db/stats/music-verify-report.md`） |
| `tools/provenance.py` | **来源台账复核器**（地址 ↔ 站点 ↔ 数据 ↔ 本地证据 ↔ 整包哈希） |
| `tools/sync_docs.py` / `tools/preflight.py` | 文档单一真源同步 / **发布前置检查**（0 失败才可发布） |

> 本声明与 `AUDIT-REPORT.md`（v1）、`AUDIT-REPORT-V2.md`（v2）、`AUDIT-REPORT-V3.md`（v3）、
> `LICENSE-AUDIT.md`、`QUALITY-GATES.md`、`PROVENANCE.md` 构成完整质量与来源档案。

## 五、版本沿革

- **v1.0（2026-09-17）**：94,740 首 / 17 源。原始声明的各项验证（含全量音乐内容统计级验证）在该时点执行。
- **现行（2026-09-21 复核）**：133,667 首 / 21 源；新增来源均逐文件解析校验（`du`/`nn` 100%）并按同一许可门槛准入；
  发布树完整性于本次复核确认（第 1 项）。分区自 v1.6 起为 main / piano-special / study 三区
  （`pending` 与 `research` 区已取消：chinafolk 经许可评估后以 C3 发布，maestro / emopia 升级为 C2 发布）。

---

## v1.23 数据增强（2026-09-23）

v1.23 新增 **2 个来源**（ATEPP 7,131 首演奏版 + PDMX 2,895 首乐谱型）与 **15 个字段**；
完整字段语义、覆盖率与标注规范见 `FIELD-DICTIONARY.md`。

**新增字段**：`difficulty` 演奏难度 · `cn_zh` 作曲家中文名 · `birth`/`death` 生卒年 ·
`instrument_gm` GM 标准乐器 · `velocity_avg`/`velocity_range` 演奏力度 · `notes_per_sec` 音符密度 ·
`pitch_range` 音域 · `midi.tempo` 速度 · `midi.timesig` 拍号 · `version_type` 版本类型（score/performance）·
`performer`/`album`（演奏版）· `topic`/`topics_zh` 题材 · 歌词库（10,035 首中国民歌，独立分片）

**标注规范**：算法推断字段一律带 `*_src`（如 `key_src="inferred"`，附 `extra.key_corr` 置信度），可筛选；
未知留空不猜（拍号推断因验证准确率 26.8% 已弃用）。
